platform :ios, '7.0'

workspace "../AppComponents.xcworkspace"

target "Sample" do
    pod "AppComponents", :path => "../AppComponents.podspec"
    pod "ESFramework", :path => "/data/projects/ESFramework/ESFramework"
end


platform :ios, '8.0'

workspace '../AppComponents.xcworkspace'

target 'Sample' do
    pod 'AppComponents', :path => '../'
    # pod 'ESFramework', :git => 'https://github.com/ElfSundae/ESFramework.git'
end


platform :ios, '7.0'

workspace "../AppComponents.xcworkspace"

target "Sample" do
    pod "AppComponents", :path => "../AppComponents.podspec"
    # pod "ESFramework" , :git => 'https://github.com/ESFramework/ESFramework.git', :branch => 'develop'
end

